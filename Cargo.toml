[package]
name = "factory_sim"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = "0.18"

serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"  # 用于 Assets
toml = "0.8"

rand = "0.8"